# Copy this file to .streamlit/secrets.toml (gitignored) for local development,
# or paste its contents into Settings → Secrets on Streamlit Cloud.
# Get a free key at https://aistudio.google.com/apikey — no billing required
# for the free tier this app uses (gemini-2.0-flash).

GEMINI_API_KEY = "your-key-here"
